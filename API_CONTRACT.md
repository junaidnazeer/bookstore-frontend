# API Contract

This file is deprecated. The current, authoritative backend API
reference lives in the `bookstore-backend` repo as `API_REFERENCE.md`.
Always confirm against that file — if anything here or there looks
outdated, treat it as a bug to report, not a contract to guess around.
